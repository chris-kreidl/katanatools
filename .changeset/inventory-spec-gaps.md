---
"@ckreidl/katana-client": patch
---

Add missing `safety_stock_level`, `default_storage_bin`, and `archived_at` fields to `KatanaInventoryItem`. Fix `quantity_potential` nullability (`string | null`).
